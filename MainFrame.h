#pragma once
#include <wx/wx.h>
class MainFrame : public wxFrame  //inherit from a class called mxFrame
{
	public:
		MainFrame(const wxString& title); //constructor for the class and takes window title as parameter type wsString
};

